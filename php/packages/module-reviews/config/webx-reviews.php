<?php

declare(strict_types=1);

return [

    /*
    |---------------------------------------------------------------------------
    | Reviews
    |---------------------------------------------------------------------------
    |
    | Nothing to set yet, on purpose: a review has no page, no address and no
    | markup of its own (decisions 2 and 3 of the reviews spec). It reaches the
    | site in a block, or through `reviews()` in a template of the site.
    |
    | The file is here so that a site has the place to publish when something
    | does become a setting, and so that `config('webx-reviews')` is an array
    | rather than null in the meantime.
    |
    */

];
