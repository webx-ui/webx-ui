---
'@webx-ui/module-blocks': patch
---

"Add a block" in a block's sample form offers only what that block lets inside, and a page field's own `allow` narrows its top level as it always said it would.
