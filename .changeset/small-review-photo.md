---
'@webx-ui/module-media': patch
'@webx-ui/php': patch
---

The media field takes a `width` that caps how wide its frame gets, and the review form uses it:
the author's photo is a 160 px square instead of a frame the full width of the form.
