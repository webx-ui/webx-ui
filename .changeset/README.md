# Changesets

This folder holds [changesets](https://github.com/changesets/changesets): one markdown file per
pending change, describing which packages to bump and why.

Add one with `pnpm changeset` in any PR that changes a published package.
