---
'@webx-ui/module-blocks': patch
---

The template editor of a block type knows what an item of the `faq`, `services` and `reviews` collections holds: `$review['` offers `name`, `initials`, `job_title`, `text`, `rating` and the rest of the card, not only `id`, `anchor` and `categories`.
