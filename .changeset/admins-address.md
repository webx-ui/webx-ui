---
'@webx-ui/module-auth': patch
---

Раздел администраторов живёт по адресу `/admins`, а не `/users`: раздел про администраторов, и адрес говорит то же самое. Идентификатор модуля остаётся `users` — его сообщает сервер, и его же несут в именах права и MCP-инструменты.
