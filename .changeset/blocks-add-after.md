---
'@webx-ui/module-blocks': patch
'@webx-ui/php': patch
---

A block's `···` menu offers "Add after": the new block goes right under that one, at the top level and inside a container alike, instead of at the end of the list.
