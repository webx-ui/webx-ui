---
'@webx-ui/php': patch
---

`module-blocks`: the section moves into the System group of the navigation, first in it, above
SEO — a block type is made once and then lives on the pages, so the section is opened the way
the settings are.
