---
'@webx-ui/php': patch
---

The blog's five public views are plain, not broken

They are the least markup that works and a site replaces them, but without `max-width: 100%` a
1200px cover pushed a phone's page out to 1248px and took every line of text off the screen with
it. Three rules in a partial the four page views include — the same three `module-pages` shows in
its own example.
