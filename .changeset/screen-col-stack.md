---
'@webx-ui/schema': patch
---

A `wx-col` on a screen stacks the fields it holds with the form's step: two fields in one column no longer stand flush, the label of the second reading as the hint of the first.
