---
'@webx-ui/module-blocks': patch
'@webx-ui/php': patch
---

A block's `···` menu offers "Move to…": the block goes whole into another container or out to the page, and only to places whose rules let it stand there.
