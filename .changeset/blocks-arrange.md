---
'@webx-ui/module-blocks': minor
'@webx-ui/php': patch
---

The cards of the Blocks section are dragged into order, and the picker offers the types in it. The order is a new `position`, apart from `sort`, which stays the order of the styles on the page (now labelled so). A migration numbers the existing types in the order they had.
