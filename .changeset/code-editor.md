---
'@webx-ui/core': minor
---

`WxCodeEditor` — a code field on CodeMirror 6: JSON (with a linter and `format()`), JavaScript, TypeScript, HTML, CSS, Markdown, YAML, PHP and plain text, styled entirely with the design tokens. The counterpart of `WxRichText` for code.
