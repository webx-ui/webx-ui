---
'@webx-ui/php': minor
---

`webx:doctor` checks a site the way a deploy needs it checked: both halves, the bundle, npm ranges, migrations, storage, the layout seam, languages, caches and limiters
