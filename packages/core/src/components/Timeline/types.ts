import type { TimelineSize } from '../../composables/useTimeline'

export interface TimelineProps {
  /** Spacing and dot size for every item in the timeline. */
  size?: TimelineSize
}

export type { TimelineSize }
